The text for this thesis, written in latex format.

The main configuration file is `thesis.tex`.
It contains the preface, used libraries and chapter layout.

The `chapters` directory contains individual chapters including appendix.

Bibliography is outlined in `bibliography.bib` file.

The latest compiled version of the text can be found in `out/thesis.pdf` directory.