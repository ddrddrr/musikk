name: musikk-local

services:
  database:
    image: postgres:17
    ports:
      - "5435:5432"
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - main

volumes:
  postgres_data:

networks:
  main: