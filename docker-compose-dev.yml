services:
  database:
    image: postgres:17
    ports:
      - "5435:5432"
    env_file:
      - src/backend/musikk/.env.prod
    volumes:
      - postgres_data:/var/lib/postgresql/data


volumes:
  postgres_data:

